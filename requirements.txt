# DÉPENDANCES SCRAPMASTER - VERSION MINIMALE FONCTIONNELLE
# Version : 2025-01-01 (Compatible Windows sans compilation)

# === Core Framework ===
Flask==3.0.0
Werkzeug

# === Web Scraping ===
requests==2.31.0
beautifulsoup4==4.12.2

# === Utilities ===
python-dateutil==2.8.2
pytz==2023.3

# === Optionnel (ajouter si besoin) ===
# langdetect    # Détection de langue (optionnel)
# phonenumbers  # Normalisation téléphones (optionnel)  
# rapidfuzz     # Déduplication (optionnel)
# lxml          # Parser XML rapide (optionnel)
# pandas        # Export Excel (optionnel - CSV sinon)
# openpyxl      # Excel (optionnel)

# Note: L'application fonctionne parfaitement avec les packages de base
